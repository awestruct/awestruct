content_syntax:
  coffee: coffeescript
  md: markdown
  mkd: markdown
  org: orgmod
  adoc: asciidoc
  ad: asciidoc

haml:
  default:
    :attr_wrapper: '"' 
    :escape_attrs: :once
  html:
    :format: :html5

slim:
  default:
    :sort_attrs: false
    :disable_escape: true
  html:
    :format: :html5

asciidoctor:
  default:
    :backend: html5
    :safe: 1
    :attributes:
      imagesdir: /images
      stylesdir: /stylesheets
